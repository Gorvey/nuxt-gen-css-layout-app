# 项目当前状态

刚刚进行了初始化,安装了一些依赖,页面创建了,还没有开始编写代码

# 项目计划

项目的核心目的是快速搭建,生成css布局代码
注意,只是为了搭建出布局代码,只是为了搭建出布局代码,只是为了搭建出布局代码
比如: 1.图片元素,不需要配置src属性2.文本元素,不需要配置文本内容,随机生成一些占位即可

我详细说说这个快速生成css样式,主要是为了解决前端开发者,在业务开发时,快速生成样式的布局
比如一个卡片布局的页面,里面是由卡片组成的,卡片拥有一些字段,左侧是图片,右侧上方是标题,下方是按钮和tag等等
如果我可以快速将这个卡片的布局写出来,复制出来后,再手动调整下样式,比手写要快很多,而且还可以保存布局,让以后的相似的布局可以快速修改使用
也就是说我不是为了快速的拖拽生成一整个页面,只是其中的一个组件,是为了方便开发者快速生成业务页面

# 页面布局

顶部是header,左侧是标题,中间是左侧的收起/展开按钮,中间编辑器区域的宽度设置,右侧面板的/展开收起按钮,右边是生成代码
下方左侧,是拖拽面板,可以拖拽容器和组件到编辑器中
中间是编辑器区域
右边是元素配置区域,可以调整元素的样式,配置等

# 核心定义

只有2种类型,一个是容器
容器: flex,grid,block
元素: 文本,分割线，图片,tag,按钮等不可分割的最小组件

# 核心需求

1. 从左侧面板通过拖拽容器和组件到中间的编辑器区域,实现快速布局
2. 在编辑器中,选中容器/元素后,右侧出现配置区域,可以调整容器/组件的样式
3. header右侧的生成按钮,点击后出现一个弹窗,可以将当前编辑器中的DSL转换为css代码
4. 弹窗中可以选择不同的css样式格式,比如css原生,tailwind

# 目前不需要实现,但是以后需要的

1. 历史记录,redo,undo
2. 保存模版

# 注意事项

1. 容器可以嵌套容器
   比如grid容器可以嵌套flex,flex可以再嵌套flex,block等容器

2. 元素只能嵌套在容器中,不能直接嵌套在其他元素中

3. 设计好DSL格式,需要考虑到容器和元素的嵌套关系,以及元素的配置项

4. 中间的编辑器区域,可以拖拽调整位置,比如从左侧拖拽进入的元素,可以调整他的位置

5.右侧元素配置区域,可以调整元素的样式,配置等,但不需要手写css代码,只暴露一些常用的样式,比如字体大小,间距,背景,边框等,并且是选择器,让用户选择预设的类型

6.最重要的就是DSL的设计,这关系到编辑器的嵌套和css代码的生成

7. 考虑到用户的使用场景,需要考虑到一些常用的布局,比如卡片布局,列表布局,网格布局等,这些布局可以直接在左侧面板中拖拽到编辑器中,用户只需要调整下样式即可

8. 目前我已经选择了一些框架进行了安装,如果还需要其他框架/库,写入到计划当中
